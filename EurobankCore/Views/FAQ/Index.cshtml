@{ 
    ViewBag.Title = ViewBag.Title;
}
@model Eurobank.Models.FAQ.FaqViewModel


<h1>@Model.Name</h1>
<editable-area area-identifier="area2" />
<div class="accordion" id="accordionExample">
    @*@foreach (var item in Model.FaqList)
    {

        <div class="card">
            <div class="card-header" id="@item.NodeId">
                <h2 class="mb-0">
                    <button class="btn btn-link btn-block text-left" type="button" data-toggle="collapse" data-target="#@item.NodeId" aria-expanded="true" aria-controls="collapseOne">
                       @item.Question
                    </button>
                </h2>
            </div>

            <div id="@item.NodeId" class="collapse show" aria-labelledby="@item.NodeId" data-parent="#accordionExample">
                <div class="card-body">
                   @Html.Raw(item.Answer)
                </div>
            </div>
        </div>

    }*@
</div>


<div class="row">
    <div class="col-md-4">@Html.Raw(Model.VisitUs)</div>
    <div class="col-md-4">@Html.Raw(Model.Email)</div>
    <div class="col-md-4">@Html.Raw(Model.ContactUs)</div>
</div>

<script>
$(document).ready(function() {
	$("#accordionExample").kendoPanelBar({
        expandMode: "single"
    });
});
</script>